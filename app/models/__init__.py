"""app.models package.

P0 STUB STRATEGY:
Every sibling model in P0 is a bare stub (table name + primary key only)
so Flask-Migrate's history starts clean.
Note: modules are imported explicitly by app/__init__.py 
"""

# print("[DEBUG] app.models package imported")